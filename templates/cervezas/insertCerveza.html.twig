{% extends 'cervezas/base.html.twig' %}

{% block body %}
<h2>Beer Advisor</h2>

    {% for message in app.flashes('Añadida') %}
        <p>{{message}}</p>

    {% endfor %}

    {{ form_start(cervezaForm) }}
        {{ form_widget(cervezaForm) }}
        <button type="submit">Añadir</button>
    {{ form_end(cervezaForm) }}

{% endblock %}